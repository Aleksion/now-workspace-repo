{
  "name": "test",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev:works": "yarn workspace test-next dev",
    "dev:fails": "now dev"
  }
}